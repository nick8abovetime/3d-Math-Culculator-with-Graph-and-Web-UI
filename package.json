{
  "name": "math-3d",
  "version": "1.0.0",
  "description": "3D Math Calculator with Jest tests",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "playwright": "^1.40.0",
    "@playwright/test": "^1.40.0"
  },
  "dependencies": {
    "mathjs": "^12.2.0"
  }
}